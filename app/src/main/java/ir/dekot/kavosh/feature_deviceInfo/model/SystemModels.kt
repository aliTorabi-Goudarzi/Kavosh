package ir.dekot.kavosh.feature_deviceInfo.model

/**
 * فایل مدل‌های سیستم
 * شامل تمام کلاس‌های داده مربوط به سیستم‌عامل، برنامه‌ها و حسگرها
 * System، App، Sensor data classes
 */

// Re-export system-related data classes for better organization
// These classes are defined in their individual files but grouped here for convenience

// System Models - defined in SystemInfo.kt (in data.model.components package)
// data class SystemInfo(...)

// App Models - defined in AppInfo.kt
// data class AppInfo(...)

// Sensor Models - defined in SensorInfo.kt
// data class SensorInfo(...)

// Camera Models - defined in CameraInfo.kt
// data class CameraInfo(...)

/**
 * این فایل به عنوان یک نقطه مرجع برای تمام مدل‌های سیستمی عمل می‌کند.
 * کلاس‌های داده واقعی در فایل‌های جداگانه تعریف شده‌اند:
 * 
 * - SystemInfo.kt: اطلاعات سیستم‌عامل (در پکیج data.model.components)
 * - AppInfo.kt: اطلاعات برنامه‌ها
 * - SensorInfo.kt: اطلاعات حسگرها
 * - CameraInfo.kt: اطلاعات دوربین‌ها
 */
