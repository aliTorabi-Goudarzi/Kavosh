package ir.dekot.kavosh.feature_deviceInfo.model

/**
 * فایل مدل‌های اتصالات
 * شامل تمام کلاس‌های داده مربوط به شبکه، سیم‌کارت و اتصالات
 * Network، SIM، WiFi data classes
 */

// Re-export connectivity-related data classes for better organization
// These classes are defined in their individual files but grouped here for convenience

// Network Models - defined in NetworkInfo.kt
// data class NetworkInfo(...)

// SIM Models - defined in SimInfo.kt
// data class SimInfo(...)

/**
 * این فایل به عنوان یک نقطه مرجع برای تمام مدل‌های اتصالات عمل می‌کند.
 * کلاس‌های داده واقعی در فایل‌های جداگانه تعریف شده‌اند:
 * 
 * - NetworkInfo.kt: اطلاعات شبکه و اتصالات
 * - SimInfo.kt: اطلاعات سیم‌کارت‌ها
 */
